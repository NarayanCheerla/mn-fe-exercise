{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "start": "npx lerna run start",
    "dev": "npx lerna run dev --scope=pokedex"
  },
  "devDependencies": {
    "lerna": "^8.1.7",
    "nx": "19.5.3",
    "concurrently": "^8.2.2",
    "tsup": "^8.2.3"
  }
}
